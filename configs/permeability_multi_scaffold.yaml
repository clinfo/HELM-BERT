# =============================================================================
# Multi-Assay Permeability - Scaffold Split
# =============================================================================
# Usage: python scripts/train_permeability_multi.py --config configs/permeability_multi_scaffold.yaml

training:
  max_epochs: 100

data:
  split_name: cycpeptmpdb_permeability_scaffold
  train_file: ./data/downstream/cycpeptmpdb_permeability_scaffold_train.csv
  test_file: ./data/downstream/cycpeptmpdb_permeability_scaffold_test.csv
