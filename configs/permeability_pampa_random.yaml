# =============================================================================
# Permeability - PAMPA only, Random Split
# =============================================================================
# Usage: python scripts/train_regression.py --config configs/permeability_pampa_random.yaml

training:
  max_epochs: 100

data:
  split_name: cycpeptmpdb_permeability_pampa_random
  train_file: ./data/downstream/cycpeptmpdb_permeability_pampa_random_train.csv
  test_file: ./data/downstream/cycpeptmpdb_permeability_pampa_random_test.csv
  target_column: PAMPA
